<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
  <title>Buy Now - Fashnova</title>
</head>

<body class="bg-gray-900 text-white min-h-screen">

  <div class="p-6 flex items-center gap-4">
    <h1 class="text-3xl font-bold bg-gradient-to-r from-purple-500 via-indigo-500 to-blue-500 text-transparent bg-clip-text">
      Fashnova
    </h1>
  </div>


  <div class="flex flex-col md:flex-row w-full px-6 md:px-20 py-6 gap-10">

    
    <div class="w-full md:w-[35%] bg-zinc-800 rounded-lg shadow-lg overflow-hidden">
      <div class="h-80 w-full bg-gray-700">
        <img class="w-full h-full object-cover" src="data:image/jpeg;base64,<%= product.image.toString('base64') %>" alt="Product Image">
      </div>
      <div class="px-5 py-4">
        <div class="flex justify-between items-center mb-4">
          <h3 class="text-2xl font-semibold"><%= product.name %></h3>
          <div class="flex items-center gap-2">
            <button class="w-8 h-8 bg-white text-black rounded-full flex items-center justify-center ri-subtract-line"></button>
            <div class="px-3 py-1 rounded bg-white text-black text-sm font-medium">01</div>
            <button class="w-8 h-8 bg-white text-black rounded-full flex items-center justify-center ri-add-line"></button>
          </div>
        </div>
        <div class="flex justify-between items-center border-t border-gray-600 pt-3">
          <h4 class="text-lg font-medium">Net Total</h4>
          <h4 class="text-lg font-semibold text-green-400">₹ <%= product.price %></h4>
        </div>
      </div>
    </div>

    
    <div class="w-full md:w-[65%] bg-zinc-800 rounded-lg shadow-lg p-6">
      <h3 class="text-2xl font-semibold mb-6">Price Breakdown</h3>
      
      <div class="space-y-4 text-lg">
        <div class="flex justify-between">
          <span>Product Price</span>
          <span>₹ <%= product.price %></span>
        </div>
        <div class="flex justify-between">
          <span>Discount on MRP</span>
          <span class="text-green-400">- ₹ <%= product.discount %></span>
        </div>
        <div class="flex justify-between">
          <span>Platform Fee</span>
          <span>₹ 20</span>
        </div>
        <div class="flex justify-between">
          <span>Shipping Fee</span>
          <span class="text-green-400">FREE</span>
        </div>
      </div>

      <hr class="my-6 border-gray-600" />

      <% let totalAmount = product.price - product.discount + 20; %>

      <div class="flex justify-between items-center mb-6">
        <h3 class="text-xl font-bold">Total Amount</h3>
        <h3 class="text-xl font-semibold text-green-500">₹ <%= totalAmount %></h3>
      </div>

      <div class="text-right">
        <a href="/placeorder/<%= product._id %>" class="inline-block bg-blue-600 hover:bg-blue-700 transition px-6 py-2 text-white rounded-md font-medium">
          Place Order
        </a>
      </div>
    </div>
  </div>
</body>
</html>
