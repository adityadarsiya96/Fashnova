<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
  <title>Place Order - Fashnova</title>
</head>

<body class="bg-gray-900 text-gray-100">
  
  <div class="bg-white shadow p-6 mb-6 flex justify-between items-center px-20">
    <h1 class="text-3xl font-bold bg-gradient-to-r from-pink-500 via-red-500 to-yellow-500 text-transparent bg-clip-text">
      Fashnova
    </h1>
    <div class="flex gap-6 text-lg font-medium">
      <a class="text-black hover:text-blue-600 transition" href="/shop">Shop</a>
      <a class="text-rose-600 hover:underline transition" href="/logout">Logout</a>
    </div>
  </div>


  <div class="w-full min-h-screen flex flex-col md:flex-row items-start justify-center px-10 md:px-20 pb-20 gap-10">

 
    <div class="w-full md:w-[40%] bg-white rounded-md shadow-lg overflow-hidden">
      <div class="w-full h-80 bg-gray-200">
        <img class="w-full h-full object-cover" src="data:image/jpeg;base64,<%= product.image.toString('base64') %>" alt="Product Image">
      </div>
      <div class="p-5 space-y-4">
        <div class="flex justify-between items-center">
          <h3 class="text-2xl text-rose-700 font-semibold"><%= product.name %></h3>
          <div class="flex items-center gap-2">
            <button class="w-7 h-7 bg-gray-300 text-black flex rounded-full items-center justify-center ri-subtract-line cursor-pointer"></button>
            <div class="px-2 py-1 rounded bg-gray-100 text-black text-sm font-medium">01</div>
            <button class="w-7 h-7 bg-gray-300 text-black flex rounded-full items-center justify-center ri-add-line cursor-pointer"></button>
          </div>
        </div>
        <p class="text-lg font-medium text-gray-700">Price: ₹<%= product.price %></p>
      </div>
    </div>

    <div class="w-full md:w-[50%] bg-white rounded-md shadow-lg p-6">
      <form action="/placeorder/<%= product._id %>" method="POST" class="space-y-6">
        <input type="hidden" name="productId" value="<%= product._id %>">
        
        <div>
          <label class="block mb-2 text-gray-800 font-semibold">Shipping Address</label>
          <textarea name="address" rows="4" required
                    class="w-full border border-gray-800 text-gray-800 rounded p-3 focus:outline-none focus:ring-2 focus:ring-blue-500 resize-none"
                    placeholder="Enter your full shipping address..."></textarea>
        </div>

        <button type="submit"
                class="w-full bg-blue-600 hover:bg-blue-700 text-white py-3 rounded text-lg font-medium transition">
          Place Order
        </button>
      </form>
    </div>

  </div>
</body>
</html>
